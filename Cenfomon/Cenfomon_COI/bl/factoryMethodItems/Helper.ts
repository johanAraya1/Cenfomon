export class Helper {

    public generarID(): number {

        return Math.floor(Math.random() * 999);
    }
    public  generarRand100():number {
        return Math.floor(Math.random()*100);
    }

}